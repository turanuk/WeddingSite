@{
  //Web endpoint that returns guest information
  Layout = null;
  var db = Database.Open("WeddingSite");
  var data = db.Query("SELECT * FROM RSVP");
  Response.AddHeader("Cache-Control", "no-cache");
  Response.Write(Json.Encode(data));
}